# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg-info/
dist/
build/

# Virtual environments
venv/
env/
ENV/

# Environment variables
.env
.env.*
!.env.example
!.env.*.example

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Database & data
data/
*.db
*.sqlite
*.sqlite3

# Confidential files
*.sql
*.key
*.pem
*.p12
*.pfx
# Secretos montados como fichero (Docker secret): carpeta versionada vacía
secrets/*
!secrets/.gitkeep
credentials/

# Frontend
node_modules/
dist/
.next/
out/

# Logs & temp
*.log
instance/
.pytest_cache/
.coverage
htmlcov/

# ETL / Migración de datos legados (privado, no va al repo público)
etl/
migration_scripts/
docs/etl_migracion_legacy.md

# Multi-agente: identidad por-worktree (local, no viaja) y carpeta de worktrees
.claude/AGENTE.local.md
.claude/settings.local.json
/SIGA-wt/

# Dumps de la BD que crea el deploy server-side (deploy-remote-pull.sh) antes de
# migrar. Son DATOS, no código: nunca van al repo (y son los que llenan el disco).
backups/
*.sql.gz