# Carpeta de Docker secrets (montados en /run/secrets en runtime).
# Se versiona vacía; su contenido (secrets/*.txt) NUNCA se versiona.
# En desarrollo (optiplex-790) no es necesaria: el código cae a env var.
